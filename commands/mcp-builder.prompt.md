---
agent: agent
description: Plan and build an MCP server with tools and deployment guidance
tools:
  - search/codebase
  - edit/editFiles
  - search
  - execute/runInTerminal
  - execute/getTerminalOutput
  - read/terminalLastCommand
  - read/terminalSelection
---
Use the skill definition at ./skills/mcp-builder/SKILL.md.
Apply it to the user request below and produce implementation-ready MCP output.
If key details are missing, ask one concise follow-up question before proceeding.

User input:
${input}
